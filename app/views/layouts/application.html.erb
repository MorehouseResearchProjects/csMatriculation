<!DOCTYPE html>
<html>
  <head>
    <title>Cs Matriculation</title>
	<%= stylesheet_link_tag 'custom' %>
    <%= csrf_meta_tags %> 
	
	<body>
	
    <div class="container">
      <%= yield %>
     
      <%= debug(params) if Rails.env.development? %>
    </div>
    
  </body>
</html>
